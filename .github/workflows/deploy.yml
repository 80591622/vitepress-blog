name: Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.repository }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy to server
    runs-on: ubuntu-latest
    if: github.repository == '80591622/vitepress-blog' && secrets.DEPLOY_SSH_KEY != ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.7.1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          HUSKY: "0"

      - name: Build site
        run: pnpm run build

      - name: Pack dist
        run: tar -zcf dist.tar.gz dist

      - name: Configure SSH
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          port="${DEPLOY_PORT:-22}"
          ssh-keyscan -p "$port" "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Upload and deploy
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_DIR: ${{ secrets.DEPLOY_DIR }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_TIMESTAMP: ${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          port="${DEPLOY_PORT:-22}"
          target="${DEPLOY_USER}@${DEPLOY_HOST}"
          ssh_opts=(-i "$HOME/.ssh/deploy_key" -p "$port" -o StrictHostKeyChecking=yes)

          scp "${ssh_opts[@]}" dist.tar.gz "${target}:${DEPLOY_DIR}/dist.tar.gz"

          ssh "${ssh_opts[@]}" "$target" \
            env \
              "DEPLOY_SERVER_DIR=${DEPLOY_DIR}" \
              "DEPLOY_TARGET_PATH=dist" \
              "DEPLOY_ARCHIVE_NAME=dist.tar.gz" \
              "DEPLOY_TIMESTAMP=${DEPLOY_TIMESTAMP}" \
            bash -s < scripts/remote-deploy.sh
